"""Stage 1-6 implementation-validation drivers: they check that the real
src/ code reproduces the behaviour the design specifies (topology load,
telemetry, decision gates, baselines, stability timers, end-to-end pilot),
as opposed to the comparative experiments under scenarios/ and cost_formula/."""
