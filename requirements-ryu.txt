# RYU controller environment -- SEPARATE from requirements.txt.
#
# ryu 4.34 needs Python 3.8-3.9 and pins old eventlet / dnspython that
# cannot coexist with the modern numpy / scipy / matplotlib stack the
# experiments and figures use. So the project runs two virtualenvs:
#
#   .venv       Python 3.11+   -- experiments, tests, figures (requirements.txt)
#   ryu-env     Python 3.9     -- only `ryu-manager` for the live controller
#                                 (scripts/ryu/, this file)
#
# Recreate:  python3.9 -m venv ryu-env && ryu-env/bin/pip install -r requirements-ryu.txt
# Neither venv is committed; exclude both from any archived copy of the project.

ryu==4.34
eventlet==0.30.2
dnspython==1.16.0
networkx==3.2.1
ovs==4.0.0
oslo.config==10.0.0
WebOb==1.8.11
Routes==2.5.1
msgpack==1.1.2
netaddr==1.3.0
tinyrpc==1.1.7
